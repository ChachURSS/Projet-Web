
<!doctype html>
<html lang="fr">
<head>
    <meta charset="utf-8">
    <title>Membres de l'organisation</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
{% include "navbar.twig" %}
<div class="container mt-5">
    <h3 class="mb-4">Liste des membres de l'organisation</h3>
    <table class="table table-hover">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Prénom</th>
                <th>Email</th>
                <th>Rôle</th>
                {% if role == 0 %}
                <th>Action</th>
                {% endif %}
            </tr>
        </thead>
        <tbody>
            {% for member in members %}
            <tr>
                <td>{{ member.last_name }}</td>
                <td>{{ member.name }}</td>
                <td>{{ member.mail }}</td>
                <td>
                    {% if member.role == 0 %}Admin
                    {% elseif member.role == 1 %}Pilote
                    {% else %}Élève
                    {% endif %}
                </td>
                {% if role == 0 %}
                <td>
                    <form method="POST" action="/organization/delete-user" onsubmit="return confirm('Supprimer ce membre ?');">
                        <input type="hidden" name="id_user" value="{{ member.id_user }}">
                        <button type="submit" class="btn btn-sm btn-danger">Supprimer</button>
                    </form>
                </td>
                {% endif %}
            </tr>
            {% endfor %}
        </tbody>
    </table>
    <a href="/organization" class="btn btn-secondary">Retour</a>
</div>
</body>
</html>
