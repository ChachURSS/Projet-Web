
<!doctype html>
<html lang="fr">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Organisation - EasyStage</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/assets/css/style.css">
    <script defer src="/assets/js/script.js"></script>
</head>
<body>
    {% include "navbar.twig" %}

    <div class="container mt-5">
        <div class="card mb-4 shadow-sm">
            <div class="row g-0">
                <div class="col-md-4 d-flex justify-content-center align-items-center bg-light">
                    <img src="{{ organization.path_to_icon }}" alt="Logo de l'organisation" class="img-fluid rounded shadow-sm" style="max-width: 200px; max-height: 200px; object-fit: contain;">
                </div>
                <div class="col-md-8">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-start">
                            <div>
                                <h3 class="card-title">{{ organization.name }}</h3>
                                <p class="card-text text-muted">{{ organization.description }}</p>
                            </div>
                            {% if role == 0 %}
                                <a href="/organization/edit" class="btn btn-sm btn-outline-secondary">Modifier</a>
                            {% endif %}
                        </div>
                    </div>
                </div>
            </div>
        </div>

        {% if role == 0 or role == 1 %}
            <div class="d-flex justify-content-between align-items-center mb-3">
                <h4>Gestion des membres</h4>
                <a href="/organization/members" class="btn btn-outline-primary">Modifier les membres</a>
            </div>

            <form method="POST" action="/organization/add-user" class="row g-3">
                <div class="col-md-4">
                    <input type="text" name="nom" class="form-control" placeholder="Nom" required>
                </div>
                <div class="col-md-4">
                    <input type="text" name="prenom" class="form-control" placeholder="Prénom" required>
                </div>
                <div class="col-md-4">
                    <input type="email" name="mail" class="form-control" placeholder="Email" required>
                </div>
                {% if role == 0 %}
                <div class="col-md-3">
                    <select name="role" class="form-select">
                        <option value="1">Pilote</option>
                        <option value="2">Élève</option>
                    </select>
                </div>
                {% else %}
                <input type="hidden" name="role" value="2">
                {% endif %}
                <div class="col-md-3">
                    <input type="password" name="password" class="form-control" placeholder="Mot de passe" required>
                </div>
                <div class="col-md-3">
                    <button type="submit" class="btn btn-primary w-100">Ajouter</button>
                </div>
            </form>
        {% endif %}
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
